import { FC, useState } from "react";
import type { TLayoutLoaderProps } from "@layout/Layout.types";

const LayoutLoader: FC<TLayoutLoaderProps> = props => {

  return (
    <div>

    </div>
  );
};

export default LayoutLoader;
